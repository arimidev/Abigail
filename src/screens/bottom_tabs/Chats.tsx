import { StyleSheet, Text, View } from "react-native";
import React from "react";
import colors from "../../utils/colors";

export const Chats = () => {
  return <View style={{ flex: 1, backgroundColor: colors.color_1 }}></View>;
};

const styles = StyleSheet.create({});
