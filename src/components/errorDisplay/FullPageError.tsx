import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const FullPageError = () => {
  return (
    <View>
      <Text>FullPageError</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
